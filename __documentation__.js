/**
 * INSTALLATION PART BY PART
 * 
 * first npm init
 * second npm i
 * create a main/index file server.js then setup initial setup. 
 * then install nodemon. then package.json file change "nodemon": "server.js"
 * install setup dotenv and cors 
 * then setup router
 * 
 * 
 * ----------------------------------------------
 * ROUTE SETUP
 * router setup to different api. at first setup auth router. and include api version. Example: /api/v1. always version api/ BASE_API_URL include .env file
 * 
 * 
 * 
 * */ 